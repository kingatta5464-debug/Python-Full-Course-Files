import atta

# if __name__ == "__main__" in Python
# This is a special block in Python used to ensure that certain code only runs when the file is executed directly, not when it's imported as a module in another file.
# we write it in the imported file
atta.welcome()
